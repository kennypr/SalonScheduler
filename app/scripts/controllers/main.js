'use strict';

/**
 * @ngdoc function
 * @name salonSchedulerApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the salonSchedulerApp
 */
angular.module('salonSchedulerApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
